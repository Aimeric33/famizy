<header class="fixed z-30 w-full bg-white shadow-xs">
  <div class="p-4 h-full flex items-center gap-4">
    <button data-action="click->menu#toggle" type="button" class="text-gray-800 md:hidden focus:outline-none" aria-label="toggle menu">
      <svg data-menu-target="burger" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 8h16M4 16h16" />
      </svg>

      <svg data-menu-target="cross" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>

    <div class="flex items-center justify-between w-full">
      <img class="w-auto h-6" src="<%= image_path("famizy-logo.svg") %>" alt="Logo Famizy">

      <div class="flex items-center" data-controller="dropdown">
        <div class="cursor-pointer relative flex items-center" data-action="click->dropdown#toggle click@window->dropdown#hide">
          <p class="hidden md:block mr-2"><%= current_user.name %></p>
          <div role="button" tabindex="0" data-dropdown-target="button" class="select-none">
            <div class="w-8 h-8 rounded-full overflow-hidden">
              <%= user_avatar(current_user) %>
            </div>
          </div>
          <div data-dropdown-target="menu"
                data-transition-enter="transition ease-out duration-200"
                data-transition-enter-from="opacity-0 translate-y-1"
                data-transition-enter-to="opacity-100 translate-y-0"
                data-transition-leave="transition ease-in duration-150"
                data-transition-leave-from="opacity-100 translate-y-0"
                data-transition-leave-to="opacity-0 translate-y-1"
                class="hidden absolute top-4 right-0 z-10 mt-5 flex max-w-max">
            <div class="bg-white shadow-xs overflow-hidden">
              <a href="#" class="block px-4 py-2 text-gray-800 bg-white hover:bg-gray-100">Account</a>
              <a href="#" class="block px-4 py-2 text-gray-800 bg-white hover:bg-gray-100">Billing</a>
              <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "block px-4 py-2 text-gray-800 bg-white hover:bg-gray-100" %>
            </div>
          </div>
        </div>
        <div class="ml-2 md:ml-0">
          <%= render 'shared/locale_switch' %>
        </div>
      </div>

    </div>
  </div>
</header>
