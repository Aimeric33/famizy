<nav class="fixed w-full z-30 bg-primary-500" data-controller="menu">
  <div class="container py-4 md:flex md:justify-between md:items-center">
    <div class="flex items-center justify-between">
      <a href="/">
        <img class="w-auto h-6" src="<%= image_path("famizy-logo.svg") %>" alt="Logo Famizy">
      </a>

      <!-- Mobile menu button -->
      <div class="flex md:hidden">
        <button data-action="click->menu#toggle" type="button" class="text-white focus:outline-none" aria-label="toggle menu">
          <svg data-menu-target="burger" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 8h16M4 16h16" />
          </svg>

          <svg data-menu-target="cross" xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

  <!-- Mobile Menu open: "block", Menu closed: "hidden" -->
    <div data-menu-target="menu" class="hidden absolute shadow inset-x-0 z-20 w-full p-4 bg-primary-500 text-center md:p-0 md:relative md:bg-transparent md:shadow-none md:w-auto md:flex md:items-center">
      <div class="flex flex-col md:flex-row md:mx-6">
        <a class="my-2 text-white transition-colors duration-300 transform hover:text-slate-200 md:mx-4 md:my-0" href="#features"><%= t('navbar.links.features') %></a>
        <a class="my-2 text-white transition-colors duration-300 transform hover:text-slate-200 md:mx-4 md:my-0" href="#pricing"><%= t('navbar.links.pricing') %></a>
        <%= render 'shared/locale_switch' %>
      </div>
      <div class="flex flex-col items-center md:flex-row">
        <a class="inline-block my-2 px-4 py-2 md:my-0 text-primary-500 transition-colors duration-300 transform bg-slate-100 rounded-md hover:bg-slate-200 focus:outline-none focus:ring focus:ring-slate-200 focus:ring-opacity-80" href="<%= families_path %>">
          <%= t('navbar.cta') %>
        </a>
      </div>
    </div>
  </div>
</nav>
