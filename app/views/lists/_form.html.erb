<div class="max-w-2xl p-5 bg-secondary-500 rounded-md">
  <%= simple_form_for [@list.family, @list] do |f| %>
    <%= f.input :name %>
    <%= f.input :list_type, as: :radio_buttons, collection: [:groceries, :to_do] %>
    <%= f.button :submit %>
  <% end %>
</div>
